# Runtime dependencies for the Flask app (used by Docker and by Vercel).
# Training-only and test-only packages live in requirements-dev.txt; the
# scheduled scraper uses the much smaller requirements-scraper.txt.

# Web
flask==3.1.2
flask-caching==2.3.1
flask-cors==6.0.2
cachelib==0.13.0
werkzeug==3.1.5
jinja2==3.1.6
markupsafe==3.0.3
itsdangerous==2.2.0
click==8.3.1
blinker==1.9.0
gunicorn==23.0.0

# Prediction. scikit-learn is pinned to the version the .joblib pipelines were
# fitted with; a different version unpickles with a compatibility warning and
# no guarantee about the result.
numpy==2.3.5
scikit-learn==1.7.2
joblib==1.5.3

# Database
sqlalchemy==1.4.54
pymysql==1.1.2
cryptography==46.0.7

# HTTP + config
requests==2.32.5
urllib3==2.6.3
certifi==2026.1.4
charset-normalizer==3.4.4
idna==3.11
python-dotenv==1.2.1
typing-extensions==4.15.0
