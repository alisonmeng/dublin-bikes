# Dependencies for the scheduled data collector (.github/workflows/scrape.yml).
# Deliberately excludes Flask and scikit-learn: the scraper talks to the APIs
# and the database only, so there is no reason to install the ML stack on
# every cron run.
requests==2.32.5
sqlalchemy==1.4.54
pymysql==1.1.2
cryptography==46.0.7
python-dotenv==1.2.1
